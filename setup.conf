# ==============================================================================
# pisugar-fx Configuration Overrides
# 
# Place this file in the same directory as setup.sh.
# The setup script will source these variables automatically.
# ==============================================================================

# The password for the badge's Wi-Fi Access Point (minimum 8 characters)
AP_PASSWORD="pisugarfx2026"

# The hostname the Pi will broadcast (mDNS will append .local)
# The Wi-Fi hotspot will automatically be named [TARGET_HOSTNAME]-AP
TARGET_HOSTNAME="ronPi"

# Set to "true" to automatically reboot when the script finishes
AUTO_REBOOT="true"